class Note {
  int category;
  String title;
  String body;
  DateTime created;
  DateTime lastUpdate;

  Note(this.category, this.title, this.body, this.created, this.lastUpdate);
}

class temp {
  int number;
  String words;

  temp(this.number, this.words);
}
